subscription OnDiscoveryJobStatusChange($jobId: ID!) {
  onDiscoveryJobStatusChange(jobId: $jobId) {
    jobId
    status
    errorMessage
    discoveredClassName
    statusMessage
    multiInstanceHint
    jobType
    currentStep
    totalDocuments
    clustersFound
    discoveredClasses
    reflectionReport
  }
}

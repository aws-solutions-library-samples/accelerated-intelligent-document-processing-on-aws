query GetTestSets {
  getTestSets {
    id
    name
    description
    filePattern
    fileCount
    source
    latestVersion
    activeReference
    labelState
    labelJobId
    labelJobStatus
    status
    createdAt
    error
    documentClassType
    configVersion
  }
}
